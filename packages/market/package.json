{
  "name": "@plumb/market",
  "version": "0.1.0",
  "private": true,
  "description": "Market data ingest — OKX public endpoints, candles and funding. Read-only, no credentials.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "fixtures"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@plumb/core": "^0.1.0",
    "better-sqlite3": "^13.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0"
  }
}
