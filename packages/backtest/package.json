{
  "name": "@plumb/backtest",
  "version": "0.1.0",
  "private": true,
  "description": "Historical replay and honest metrics. No result is claimed until it produces one.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@plumb/core": "^0.1.0",
    "@plumb/market": "^0.1.0",
    "@plumb/strategy": "^0.1.0",
    "@plumb/risk": "^0.1.0"
  }
}
